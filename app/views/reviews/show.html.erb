<p id="notice"><%= notice %></p>

<div class = "container" id = "default-padding">
    <div class = "panel panel-default">
	<div class = "panel-body">
	    <h3><%= @review.subject.name %></h3>
	    <p><i><%= @review.professor.last_name + ", " @review.professor.first_name %></i></p>
	    <p><%= @review.desc %></p>
	</div>

	<!-- SCORE -->
    </div>
</div>
